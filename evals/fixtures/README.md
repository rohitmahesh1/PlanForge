# Fixtures

This directory is for deterministic fixture data used by eval adapters.

The first workflow-classification slice does not need live calendar fixtures, but the structure is here for the next stage:

- mock freebusy snapshots
- mock event search results
- mock task lists
- canned tool traces

The sample JSON file in this folder shows the shape future router and sandbox evals can use.
